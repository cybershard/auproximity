// used for socket.io cors on development as the hot-reload is served from a different origin
export const DEV_WEBUI_HOSTNAME = "localhost";
export const DEV_WEBUI_PORT = 8080;

export const SERVER_PORT = 8079;
export const IMPOSTOR_BACKEND_PORT = 22044;
